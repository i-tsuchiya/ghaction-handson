name: Hello

on:
  workflow_dispatch:
    inputs:
      greeting:
        type: string
        default: Hello
        requied: true
        description: A cheerful word
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
    - run: echo "${{ inputs.greeting }}"
